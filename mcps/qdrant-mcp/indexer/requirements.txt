qdrant-client>=1.9.0
# Pinned to the verified working set (CPU image). Keep in lockstep with
# requirements-gpu.txt to avoid silent drift on rebuild.
fastembed==0.8.0
onnxruntime==1.27.0
pyyaml>=6.0
watchdog>=4.0.0
tree-sitter>=0.23.0
tree-sitter-language-pack>=0.7.0
tokenizers>=0.20.0
